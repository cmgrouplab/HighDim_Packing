int eventspercycle = 20;  // # events per sphere per cycle
int N = 500;                         // number of spheres
double initialpf = 0.1;                   // initial packing fraction
double maxpf = 0.8;                   // max packing fraction
double temp = 0.1;    // initial temp., use 0 for zero velocities
double growthrate = 0.01;          // growth rate
double maxpressure = 45.;           // max pressure 
char* readfile = new                   // if new, creates new packing
char* writefile = write.dat  // after mp2
char* datafile = stats.dat  // data file up to mp2
